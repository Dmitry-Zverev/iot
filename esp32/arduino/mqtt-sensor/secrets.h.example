// Скопируй в secrets.h и заполни. secrets.h в git не попадает.
#pragma once

#define WIFI_SSID       "MyWiFi"
#define WIFI_PASSWORD   "wifi-password"

// IP машины с docker compose. НЕ localhost — для ESP32 это он сам.
//   Windows: ipconfig      |  macOS: ipconfig getifaddr en0
#define MQTT_HOST       "192.168.1.50"
#define MQTT_PORT       1883
#define MQTT_USER       "iot"
#define MQTT_PASSWORD   "возьми MQTT_PASSWORD из .env"
